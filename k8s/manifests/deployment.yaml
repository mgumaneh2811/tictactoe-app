apiVersion: apps/v1
kind: Deployment
metadata:
  name: ttt-deployment
  labels:
    app: ttt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ttt
  
  template:
    metadata:
      labels:
        app: ttt
    spec:
      containers:
      - name: ttt
        image: mgumaneh2811/ttt_app:latest
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "0.5"
            memory: "512Mi"
          requests:
            cpu: "0.2"
            memory: "256Mi"
